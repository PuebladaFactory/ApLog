import { FormatoNumericoDirective } from './formato-numerico.directive';

describe('FormatoNumericoDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatoNumericoDirective();
    expect(directive).toBeTruthy();
  });
});
