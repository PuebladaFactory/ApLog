import { FormatoMonetarioControlDirective } from './formato-monetario-control.directive';

describe('FormatoMonetarioControlDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatoMonetarioControlDirective();
    expect(directive).toBeTruthy();
  });
});
