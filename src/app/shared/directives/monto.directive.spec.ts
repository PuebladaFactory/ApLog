import { MontoDirective } from './monto.directive';

describe('MontoDirective', () => {
  it('should create an instance', () => {
    const directive = new MontoDirective();
    expect(directive).toBeTruthy();
  });
});
