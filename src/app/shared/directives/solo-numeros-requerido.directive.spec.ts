import { SoloNumerosRequeridoDirective } from './solo-numeros-requerido.directive';

describe('SoloNumerosRequeridoDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloNumerosRequeridoDirective();
    expect(directive).toBeTruthy();
  });
});
