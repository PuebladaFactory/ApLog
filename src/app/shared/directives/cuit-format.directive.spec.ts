import { CuitFormatDirective } from './cuit-format.directive';

describe('CuitFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CuitFormatDirective();
    expect(directive).toBeTruthy();
  });
});
