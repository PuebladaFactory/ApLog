import { CalcularPorcentajeV2Pipe } from './calcular-porcentaje-v2.pipe';

describe('CalcularPorcentajeV2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularPorcentajeV2Pipe();
    expect(pipe).toBeTruthy();
  });
});
