import { FormatearValorPipe } from './formatear-valor.pipe';

describe('FormatearValorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearValorPipe();
    expect(pipe).toBeTruthy();
  });
});
